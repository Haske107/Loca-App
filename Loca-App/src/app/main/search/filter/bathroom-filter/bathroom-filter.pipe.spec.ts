import { BathroomFilterPipe } from './bathroom-filter.pipe';

describe('BathroomFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BathroomFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
