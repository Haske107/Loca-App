import { ElectricityFilterPipe } from './electricity-filter.pipe';

describe('ElectricityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ElectricityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
